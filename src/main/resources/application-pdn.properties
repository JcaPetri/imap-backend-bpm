# Production profile — sensitive values via environment variables
# Activado con SPRING_PROFILES_ACTIVE=pdn (ver systemd unit del VPS).

# ── Datasource ────────────────────────────────────────────────────────────────
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.show-sql=false

logging.level.com.imap.bpm=INFO

# ── CORS ─────────────────────────────────────────────────────────────────────
imap.cors.allowed-origins=https://imaps.com.ar

# ── JWT ─────────────────────────────────────────────────────────────────────
# Mismo secret simétrico que IAM productivo emite (HS512).
# Setear JWT_ACCESS_SECRET en /opt/imap/bpm/.env del VPS.
jwt.access.secret=${JWT_ACCESS_SECRET}

# ── Actuator + Prometheus ───────────────────────────────────────────────────
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.metrics.tags.application=imap-bpm
management.metrics.tags.environment=pdn

# 404 → handler en lugar de Spring default
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# ─── ServiceTaskRegistry: en pdn los microservicios viven en localhost (mismo VPS) ──
# Habilitada Fase 3 Día 5: inventory ya está deployed en :8094 con handlers reales.
# IMPORTANTE: el ServiceTaskRegistry inyecta el Map via SpEL `#{${prop:{:}}}` que
# REQUIERE formato literal de map ({k:'v',...}), no dotted properties (las dotted
# no se agregan automáticamente al Map).
bpm.service-tasks.remotes={inventory:'http://localhost:8094/imap/inventory',system:'http://localhost:8092/imap/system',manufacturing:'http://localhost:8095/imap/manufacturing'}
bpm.service-tasks.retry.max-attempts=3
bpm.service-tasks.retry.backoff-ms=1000
bpm.service-tasks.timeout-seconds=30
